# Production Environment Configuration
# Secure configuration for production deployment

# =============================================================================
# PRODUCTION SETTINGS
# =============================================================================

# Disable debug mode in production
DEBUG=false

# Use high-quality embedding models for production
EMBEDDING__MODEL_TYPE=openai
EMBEDDING__MODEL_NAME=text-embedding-3-small
EMBEDDING__BATCH_SIZE=50
EMBEDDING__MAX_RETRIES=5
EMBEDDING__TIMEOUT_SECONDS=120

# Optimized text processing for production
TEXT_PROCESSING__CHUNK_SIZE=1200
TEXT_PROCESSING__CHUNK_OVERLAP=200
TEXT_PROCESSING__MIN_CHUNK_SIZE=100

# Production ChromaDB configuration
CHROMA_DB__PERSIST_DIRECTORY=/data/chroma_db
CHROMA_DB__COLLECTION_NAME=production_documents
CHROMA_DB__MAX_RESULTS=20

# Production PDF processing limits
PDF__MAX_FILE_SIZE_MB=100
PDF__TIMEOUT_SECONDS=600

# Production logging configuration
LOGGING__LEVEL=INFO
LOGGING__FILE_PATH=/var/log/pdf_vector_system.log
LOGGING__ROTATION=50 MB
LOGGING__RETENTION=30 days

# Optimized worker configuration for production
MAX_WORKERS=8

# =============================================================================
# REQUIRED API KEYS (set these in your environment)
# =============================================================================

# OpenAI API Key (required for production embedding model)
OPENAI_API_KEY=${OPENAI_API_KEY}

# =============================================================================
# OPTIONAL API KEYS (for additional providers)
# =============================================================================

# Cohere API Key (optional)
# COHERE_API_KEY=${COHERE_API_KEY}

# Google Gemini API Key (optional)
# GOOGLE_GEMINI_API_KEY=${GOOGLE_GEMINI_API_KEY}

# Google Cloud Project (for Vertex AI)
# GOOGLE_CLOUD_PROJECT=${GOOGLE_CLOUD_PROJECT}
# GOOGLE_APPLICATION_CREDENTIALS=${GOOGLE_APPLICATION_CREDENTIALS}

# =============================================================================
# SECURITY SETTINGS
# =============================================================================

# Ensure secure file permissions
# chmod 600 .env

# Use environment variable substitution for sensitive values
# Never commit actual API keys to version control
